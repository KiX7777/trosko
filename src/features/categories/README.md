# Categories

Categories classify income and expenses and may carry an icon, color, and optional parent category. The page supports create, edit, and delete operations. Category mutations invalidate category, dashboard, transaction, and recurring queries because those screens render category names and aggregates.
