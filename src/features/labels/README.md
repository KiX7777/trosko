# Labels

Labels are lightweight user-defined tags for transactions. The page creates labels and shows their usage by looking at all transactions. Label filtering and assignment are handled in the transactions UI, while persistence is exposed through `use-label-queries.ts`.
