# Accounts

Accounts represent the places money is held: current accounts, cards, savings, cash, wallets, or other sources. The page supports creation, editing, archiving, and manual balance updates.

Creating an account initializes its current balance from `initialBalance`. Transactions and recurring auto-logs subsequently change the balance. Archiving hides an account from the default list without destroying its historical relationship to transactions.
