# Server common utilities

This directory is reserved for server-wide concerns such as normalized API errors and cross-module helpers. Keep module-specific behavior inside its module so the HTTP boundary remains discoverable.
